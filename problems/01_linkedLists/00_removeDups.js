// Write code to remove all duplicates from an unsorted linked list.
// How you you solve this problem if a temporary buffer is not allowed?

function removeDups() {
  
}

if(typeof exports !== 'undefined') {
    module.exports.removeDups = removeDups;
}