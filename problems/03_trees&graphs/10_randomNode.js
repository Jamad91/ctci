// You are implementing a binary trees class from scratch which, in addition to insert, find, and delete, has a method getRandomNode() which returns a random node from the trees
// All nodes should be equally likely to be chosen
// Design and implement an algorithm to getRandomNode and explain houw you would implement the rest of the methods

function randomNode() {
  
}

if(typeof exports !== 'undefined') {
    module.exports.randomNode = randomNode;
}