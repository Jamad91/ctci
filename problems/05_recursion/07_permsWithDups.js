// Write a method to compute all permutaions of a string whose characters are not neccessarily unique. 
// The list of permutaions should not have duplicates

function permsWithDups() {
  
}

if(typeof exports !== 'undefined') {
    module.exports.permsWithDups = permsWithDups;
}