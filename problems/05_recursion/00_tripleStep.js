// A child is running up a staircase with n steps and can hop either 1 step, 2 step, or 3 steps at a time.
// Implements a method to count how many possible ways the child can run up the stairs.

function tripleStep() {
  
}

if(typeof exports !== 'undefined') {
    module.exports.tripleStep = tripleStep;
}