var chai = require('chai');
let assert = chai.assert
let expect = chai.expect
let should = chai.should()
let successor = require('../../problems/03_trees&graphs/05_successor.js').successor

describe('successor', function() {
  
})